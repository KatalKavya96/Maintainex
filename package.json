{
  "name": "maintainex",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace maintainex-client",
    "vercel-build": "npm run prisma:generate --workspace maintainex-server && npm run build --workspace maintainex-client"
  },
  "dependencies": {
    "next": "^14.2.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
